<%# default partial to display solr document fields in catalog show view -%>
<div id="toc" style="display: none;">
  <%= FileServer.toc(document.id) %>
</div>
<% document_empty ||= !FileServer.has_text(document.id) %>
<% if !document_empty %>
    <div class="text-content">
      <div class="worksearch" style="display: none;">
        <%= render partial: 'work_search', locals: {document: document} %>
      </div>
      <div class="text">
        <% text ||= FileServer.render_snippet(document.id) %>
        <%= text.html_safe %>
      </div>
    </div>
<% else %>
    <div>
      <%= t '.no_text' %>
    </div>
<% end %>